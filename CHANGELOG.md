# 更新日志

## v1.2.5 — .md 导出增强 + 数据统一 + 手坑显示分级 + 安全修复

### ✨ 新增功能

- **导出 .md 文件末尾追加 AI 分析规范附录** — 导出报告尾部自动附加字段对应表、分析方法说明、HTML 报告样式指南和章节结构，AI 拿到 .md 文件即可直接生成风格统一的 HTML 报告，无需额外配置
- **旧字段自动迁移** — 应用启动时自动将旧格式手坑数据（散落的 `gotDroll`/`gotLancea` 等 boolean 字段）统一转换为新的 `handtraps` 数组格式，一次迁移永久生效
- **基础手坑字段独立保障** — 即使手坑预设被删除，`gotMaxxc`/`gotDroll`/`gotJellyfish`/`gotLancea`/`gotNibiru`/`gotDimension` 六种基础字段数据也不会丢失，始终在统计和导出中完整呈现

### 🔧 导出改进

- **手坑表简化** — 取消上下半部划分，统一为一个表格；"小手坑"更名为"其他手坑"；已删除预设的字段行末标注 ⚠️
- **附录字段表精简** — 只保留前端显示名 ↔ 后端字段ID 对应关系，去除实卡名称推测
- **导出附录新增卡手类型说明** — 每种卡手类型附带详细描述，方便 AI 理解数据含义

### 🔧 显示改进

- **手坑三级分级全模块统一** — 大字显示（large）/ 普通显示（compact）/ 归入其他手坑 三级逻辑在饼图、统计面板、先后手细分、晋级/保级赛手坑详情、轮播面板中全部同步实现
- **卡手类型命名统一** — 全模块统一：`卡组件`→**卡废件**、`卡复数`→**卡同名牌**、`卡手坑`→**卡后手牌**，并更新说明文字

### 🔧 统计改进

- **晋级/保级赛手坑配置同步** — `computeTypeStats` 返回值补充 `config` 字段，使晋级赛/保级赛的手坑摘要也能读取显示级别配置

### 🔒 安全修复

- **5 处 HTML 转义遗漏修复** — 统计面板、图表面板、轮播面板、晋级/保级赛手坑摘要中 handtrap 标签以及 matchup 图表中的卡组名直接拼入 innerHTML 时未调 `escapeHtml`，存在存储型 XSS 风险。现已全部修复。

---

### ✨ 新增功能

- **导出 .md 文件末尾追加 AI 分析规范附录** — 导出报告尾部自动附加字段对应表、分析方法说明、HTML 报告样式指南和章节结构，AI 拿到 .md 文件即可直接生成风格统一的 HTML 报告，无需额外配置
- **旧字段自动迁移** — 应用启动时自动将旧格式手坑数据（散落的 `gotDroll`/`gotLancea` 等 boolean 字段）统一转换为新的 `handtraps` 数组格式，一次迁移永久生效
- **基础手坑字段独立保障** — 即使手坑预设被删除，`gotMaxxc`/`gotDroll`/`gotJellyfish`/`gotLancea`/`gotNibiru`/`gotDimension` 六种基础字段数据也不会丢失，始终在统计和导出中完整呈现

### 🔧 导出改进

- **手坑表简化** — 取消上下半部划分，统一为一个表格；"小手坑"更名为"其他手坑"；已删除预设的字段行末标注 ⚠️
- **附录字段表精简** — 只保留前端显示名 ↔ 后端字段ID 对应关系，去除实卡名称推测（如"对应卡牌：古遗物-圣枪"等）

### 🔧 显示改进

- **手坑三级分级全模块统一** — 大字显示（large）/ 普通显示（compact）/ 归入其他手坑 三级逻辑在饼图、统计面板、先后手细分、晋级/保级赛手坑详情、轮播面板中全部同步实现，被归入其他手坑的预设不单独出现，其数据累加到"其他手坑"计数中

### 🔧 统计改进

- **晋级/保级赛手坑配置同步** — `computeTypeStats` 返回值补充 `config` 字段，使晋级赛/保级赛的手坑摘要也能读取显示级别配置

---

## v1.2.4 — 导出 .md 统计报告 + 导出增强 + 算法修正

### ✨ 新增功能

- **导出 .md 统计报告** — 详细统计面板标题栏新增「📝 .md」按钮，可将当前时间范围的完整统计导出为 Markdown 文件
  - 报告包含全部 20+ 项统计指标的表格数据，每项附算法说明
  - 对局明细表包含全部原始记录（16 列），可直接拖入 AI 进一步分析

### 🔧 导出改进

- **手坑字段修复** — 旧格式对局的手坑数据（独立 boolean 字段）现在能被正确导出，不再显示为空白；统一使用 `getMatchHandtraps()` 兼容新旧两种格式
- **对局明细表扩展至 16 列** — 新增「类型」「对手」「状态」「其他」4 列，覆盖晋级/保级、对手大牌/吓跑/T0、掉线/超时、提丰/抽干等所有字段
- **卡手字段细化** — 从 Y/— 细化为具体类型：动不了/卡组件/卡复数/卡手坑/互卡
- **字段说明附录** — 报告末尾添加字段说明表，列出每个字段的含义、所属统计范畴和取值说明

### 🔧 算法修正

- **先手终场统计** — 对手投降判定不再仅限 endboardState=stopped，只要 opponentRan/对手掉线/对手超时/对手抽干 且结果为胜，均计为对手投降
- **吓跑对手算法** — 增加「无终场」分类；先手+对手吓跑+无终场也计为对手投降
- **对手T0字段描述** — 修正为"对手在自己先攻回合发动效果（手坑/特殊召唤等）"

### ✨ 新增功能

- **导出 .md 统计报告** — 详细统计面板标题栏新增「📝 .md」按钮，点击弹出系统文件保存对话框，可将**当前时间范围**的完整统计导出为 Markdown 文件
  - 报告包含全部 20+ 项统计指标的表格数据（概览、先后手、硬币、手坑、卡手、失误、吓跑、T0、终场、突破、连接、大牌/提丰/抽干、卡组统计、交叉统计、晋级/保级赛）
  - **每个统计项附有算法说明**（`> **算法**: ...`），明确每项的计算逻辑
  - **对局明细表包含全部原始记录**（无行数限制），可直接拖入 AI 进行进一步分析
  - 调用 `dialog.showSaveDialog`，用户可自由选择保存路径和文件名

---

### 🔒 安全修复

- **XSS 防御** — 所有用户文本字段（卡组名、备注、预设标签）渲染时自动 HTML 转义，图表 info 面板核心函数 `infoRow()` / `infoBar()` 自带转义
- **`shell.openExternal` URL 白名单** — 仅允许打开 `github.com` 和 `raw.githubusercontent.com` 的 `https:` 链接
- **CSP 内容安全策略** — 所有页面限制资源仅同源加载，阻止外部脚本注入
- **导入校验** — 导入仅覆盖对局数据，不覆盖预设配置；限制最大 10 万条
- **输入长度限制** — 所有预设名称上限 100 字符

### 🔐 数据加密

- **自包含 AES-256-GCM 加密** — 数据文件自动加密存储，密钥嵌入文件自身，复制到任何机器可直接解密，无需独立密钥文件
- **旧版兼容** — 自动识别新版加密、旧版密钥文件加密、未加密三种格式，无感迁移

### 🔧 改进

- **统计代码模块化** — 核心统计函数按类别拆分为 15 个独立模块，降低维护难度
- **`crypto.randomUUID()`** — 对局 ID 改用真正的 UUID
- **版本号统一** — `start.bat` 从 `package.json` 动态读取，消除多处硬编码不一致
- **单元测试** — 新增 `test/stats-core.test.js`（26 用例），`npm test` 即可运行
- **构建产物清理** — `build-log.txt` 不再提交到版本控制

---## v1.2.0 — 安全审计修复：XSS 防御 + CSP + 数据校验

### 🔒 安全修复

- **XSS 跨站脚本注入** — 所有前端页面中对用户可控文本字段（卡组名、备注、预设标签）的 innerHTML 拼接添加 HTML 转义（`escapeHtml`）。核心工具函数 `infoRow()` / `infoBar()` 新增 `escapeHtml`，一次性覆盖全部图表 info 面板
- **`shell.openExternal` URL 白名单** — 限制可打开的外部链接仅为 `github.com` 和 `raw.githubusercontent.com`，且仅允许 `https:` 协议
- **CSP 内容安全策略** — 所有 5 个 HTML 文件添加 `Content-Security-Policy` meta 标签，限制资源仅同源加载，阻断外部脚本注入
- **导入数据校验增强** — `import-json` 不再覆盖整个 `data` 对象，改为仅覆盖 `matches`，并限制最大 10 万条
- **输入长度限制** — 卡组预设名称、手坑预设名称添加 100 字符上限

### 🔧 改进

- **ID 生成改为 `crypto.randomUUID()`** — 替代 `Math.random()` 实现真正的唯一 ID
- **版本号统一** — `start.bat` 改为从 `package.json` 动态读取版本号，数据源统一
- **`build-log.txt` 移出版本控制** — 加入 `.gitignore`，构建日志不再提交

---

## v1.2.0 — 自定义手坑预设 + 录入面板重构 + 双方都动不了投降

### ✨ 新增功能

- **自定义手坑预设** — 像管理卡组预设一样管理手坑名称列表，增删手坑、调整显示级别（大字体/简略/隐藏），环境变化随时适配
- **对手主动投降** — 对手跑路+没做出来不再算入「没做出来」，单独统计为「对手先投降」
- **双方都动不了→投降选项** — 双方动不了时可选「有人投降」→选择自己/对方投降，自动绑定胜负结果并锁定另一按钮
- **轮播面板手动翻页** — 暂停播放后显示 ◀ ▶ 按钮，支持手动切换幻灯片
- **手坑录入折叠式** — 「吃到手坑」「卡手」等收进主 checkbox，展开后才显示子选项，节省空间
- **掉线/超时/提丰/抽干子菜单按钮组** — 统一改为按钮组风格，取消主项时自动清理子项状态

### 🔧 改进

- **互卡独立统计** — 从「被发牌员制裁」中移出，与卡废件/卡同名/卡后手牌并列展示
- **吃G率聚合移除** — 增殖G/鸟G/水母G不再合并计算，全部按独立手坑展示
- **掉线与超时互斥** — 同时只能选其一，选中自动取消另一项
- **对手跑了/掉线/超时/抽干互斥** — 四项互斥，选中任一项自动取消其余
- **终场/突破投降与对手跑/双方动不了互斥** — 投降时自动清除对手跑和双方动不了
- **后手突破页面风格统一** — 改为与先手终场一致的 3 列网格布局
- **后场突破子菜单级联清理** — 每级子选单隐藏时自动清理状态和按钮高亮
- **提交校验增强** — 单选子选单展开无选中时弹提示；多选子选单无选中时自动折叠
- **图表面板/轮播面板索引安全过滤** — 移除渲染器时自动跳过对应项，防止导航错乱

### 🐛 修复

- **时间范围选择器无法选中非今天日期** — `refreshTimeRangeUI` 强制弹回今天，已修复
- **双方动不了与完全动不了错误联动** — 已解除关联
- **手坑管理弹窗按回车触发外部提交** — 排除 `handtrap-new-name`
- **投降按钮互斥时选择器不存在** — `#result-btns` 选择器修正
- **CSS 内联 display:none 与 class 冲突** — 条件区域改用纯 class 控制
- **多个子菜单隐藏后按钮高亮残留** — 全部补清除逻辑

---

## v1.2.2 — 异常提交后复选框未恢复 + 自用卡组必填校验

### 🐛 修复

- **异常提交后对局复选框未恢复显示** — 选择异常时对局条件区域隐藏（正常逻辑），但点击确认提交后未取消隐藏，导致连续录入时复选框消失。`clearForm()` 缺少恢复 `section-match-conditions` display 的代码，已补全
- **自用卡组为空时可跳过提交** — 与对手卡组不同（对手可能未知），自用卡组用户理应知晓。新增必填校验：未选择自用卡组时弹出提示并阻断提交

---

## v1.2.1 — 异常独立toggle + 双方都动不了子选项 + 胜负自动锁定

### ✨ 新增功能

- **先手终场「直接胜利」扩展** — 对方跑/掉线/超时/抽干牌库四种情况统一归入「直接胜利」，不再计入「没做出来」
- **掉线/超时/抽干→胜负自动锁定** — 勾选并选择方后自动绑定胜负，平局同步锁定，逻辑同双方都动不了中的投降选项
- **异常独立 toggle** — 从结果按钮移出为独立开关，选中时隐藏所有对局条件并锁定胜负
- **双方都动不了子选项** — 新增「有人先动」「有人投降」「其他」三项细分，特殊情况（掉线/超时/抽干/对手跑）时自动联动选中「其他」
- **互卡子选项统计** — 统计面板新增双方都动不了子选项详情卡片

### 🔧 改进

- **终场统计标签更名** — "对手先投降" → "直接胜利"
- **结果按钮宽度统一** — 胜/负/平局与硬币、先后手按钮宽度一致
- **异常开关视觉优化** — 红色分隔线 + 选中高亮，独立于胜负按钮组
- **晋级/保级赛手坑兼容** — 手坑统计改用 `getMatchHandtraps()` 读取，兼容新旧数据格式
- **双方都动不了子选单居左** — 移至对应复选框下方，CSS 占满整行
- **统计面板滚动条** — 自定义宽度/颜色/圆角，避开标题栏

### 🐛 修复

- **互斥切换后锁定残留** — 掉线→超时切换后锁定未解除
- **双方都动不了子选单高亮残留** — 取消勾选后按钮 active 状态未清除
- **双方都动不了与对手跑了勾选冲突** — 勾选彼此未互斥清除
- **晋级赛/保级赛手坑统计为零** — 新格式数据的手坑字段未被正确读取
- **结果按钮切换时异常状态未互斥清除
- **互卡子选项详情统计遗漏旧版数据** — 旧版 `firstMover` 字段存储 `"self"`/`"opponent"`，新版改为三分类 `"move"`/`"surrender"`/`"other"`。统计仅识别新版格式，导致旧数据未计入「有人先动」或「其他」而丢失。已兼容旧格式，按「有人先动」+谁先动处理，历史记录同步修正**

---

## v1.1.8 — 跨日日期刷新 & 手动刷新按钮

### 🐛 修复
- **「一天」跨日不刷新** — 23:59 点选「一天」后跨到次日，「今天」标签未更新。现在点击「↻」按钮可手动刷新一天/一周/一月的日期范围，录入对局后也会自动刷新。

### ✨ 新增功能
- **手动刷新按钮** — 日期选择器内新增「↻」按钮，点击后自动检测并更新「今天」/本周/本月范围。

---

## v1.1.7 — 卡手率面板新增各卡组卡手明细

### ✨ 新增功能

- **卡手率面板新增各卡组卡手明细** — 在详细统计面板（stats.html）的卡手率卡片底部新增「各卡组卡手明细」区域，按自用卡组分别展示：
  - 对局数、卡手率（带绿/黄/红三色进度条）
  - 细分：卡手/废件/同名/后手/互卡 次数
- **图表面板卡手率页同步新增** — 图表面板卡手原因分布页的右侧信息面板底部新增「各卡组卡手率」进度条列表

### 🔧 后端

- `main.js` computeStats() 新增 `handState.byDeck` 统计，按自用卡组聚合对局数、卡手次数、各细分原因次数、胜负场、卡手率和胜率

## v1.1.6 — 硬币异常检测阈值修正 + 卡手统计先后手细分 + 全局机制统一

### 🔧 改进

- **硬币异常检测标签逻辑修正** — `severity` 标签从基于 pValue/z-score 阈值改为基于 `severityScore`（与展示颜色阈值对齐），现在 severityScore=92% 正确显示「🔥 异常」而非「正常」
- **硬币偏斜检测样本下限调整** — 偏斜检测从 n<15 保护改回 n≥10 正常判定，与用户确认后保持一致
- **全局颜色方案统一** — 三面板（轮播/统计/图表）低严重度颜色全部统一为绿色 `#4cd964`，消除轮播用灰色、图表用绿色的不一致
- **stats.html 偏斜正面率阈值统一** — 从独立 `|pct-50|` 阈值改为统一的 `severityScore` 体系
- **区块标题统一** — stats.html "📊 硬币随机性检测"/"📐 硬币偏斜检测" → "连续异常检测"/"偏斜异常检测"

### ✨ 新增功能

- **卡手统计先后手细分** — `main.js` handState 新增 `byFirst`/`bySecond` 子对象，三项面板同步更新：
  - **详细统计面板** — 每项卡手原因（卡废件/卡同名牌/卡后手牌/完全动不了/互卡）均显示先手 N/总数 · 后手 N/总数，含占比
  - **轮播面板** — 卡手率新增「先手 N 次 · 后手 N 次」行；被制裁面板每项下方显示「先 N · 后 N」
  - **图表面板** — info 面板每项卡手原因含先后手计数，卡手率分全局/先手/后手三条进度条

### 🖼️ 展示优化

- **轮播硬币异常检测字体缩小** — 严重度分数 22px→17px，正面率 32px→26px，防止内容溢出面板
- **图表面板硬币异常检测柱状图标签提亮** — 分数文字改用白色 + 黑色阴影（不再使用柱体颜色），严重度标签 11px→12px 加粗 + 阴影，X/Y 轴刻度改为白色
- **图表面板其他X轴字体提亮** — X轴刻度色值改为 `#ffffff`，保持可读性

## v1.1.5 — 对手跑细分 + 先后手差分全覆盖 + 胜率颜色标识

### ✨ 新增功能

- **对手跑细分统计** — 统计/轮播/图表面板新增对手跑时的场面细分：先手终场类型（正常/妥协/没做出来）和后手突破类型（不需突破/成功突破/没突破成），记录对手在什么场面下逃跑
- **先后手差分全覆盖** — 吃手坑情况、遇到大牌哥统计全面加入先后手细分显示（7 种手坑 × 先后手），匹配已有的大牌哥先后手细分
- **详细统计面板晋级/保级赛对手卡组全部显示** — 不再限制前 5 个，显示所有对手卡组分布

### 🔧 改进

- **投降→自动负+锁定** — 终场/突破中选择投降时自动设结果为负，禁用胜按钮，同时清除对手跑标记（投降与对手跑互斥）
- **先手对手跑时不再隐藏终场选项** — 对手跑后仍可选择终场类型（记录对手跑时的场面状态），相关清除逻辑移除
- **胜率数字颜色标识** — 所有面板（统计/轮播/图表）中的胜率数字按 >50%绿色 / <50%红色 / =50%白色 动态着色，大数字（big-value）保持原有固定配色
- **吃G率文字说明和格式统一** — 标注改为「一场对局中吃到G+鸟G+水母G」，顶部吃G率增加具体次数显示，先手/后手吃G率改用进度条
- **面板宽度和置顶** — 详细统计面板加宽至 1100px 并置顶，控制面板加宽至 580px
- **详细统计面板条目对齐** — 所有 stat-row 标签固定最小宽度 185px，数值/进度条防挤压
- **图表/统计面板 X 轴标注移除** — 胜率趋势和硬币胜率趋势曲线图的 X 轴数字标注全部隐藏
- **牌组抽干页去除胜率** — 轮播/统计/图表面板中自行抽干的胜率信息移除（谁抽干谁肯定输）
- **轮播面板对手跑文案优化** — 没做出来跑→没做出来也跑，不须突破跑→不需突破时跑，成功突破跑→成功突破后跑

### 🐛 修复

- **对手跑状态残留** — 投降时对手跑标记未清除导致录入数据异常
- **手坑先后手细分补全** — 轮播面板细分行仅显示 G/鸟/水母/锁，缺少陨石/宇宙人/其他
- **吃陨率移除** — 主面板去掉吃陨率行（陨石出现频率极低）
- **吃G率与细分顺序** — 总的吃G率/吃陨率移至先后手细分上方

## v1.1.4 — 硬币异常检测：连续异常率 + 偏斜异常率

### ✨ 新增功能

- **硬币异常检测（连续+偏斜双维度）** — 统计面板硬币卡片新增「硬币随机性检测」和「硬币偏斜检测」，分别检测：
  - 连正/连反异常：统计最长连续同面是否超出纯随机期望值
  - 正反偏斜异常：检测整体正反比例是否显著偏离 50%（即使没有长连续也能发现）
- **图表面板新增「硬币异常检测」** — 双项水平柱状图，直观展示连续异常率和偏斜异常率（0~100%），附带详细数据面板
- **轮播面板新增「硬币异常检测」** — 一页展示硬币正反比例、连续异常率、偏斜异常率及当前/最长连续
- **循环显示可选页新增** — 配置面板中新增「硬币异常检测」选项，共 27 个可选页面

### 🔧 改进

- **图表面板顺序调整** — 硬币统计 → 硬币胜率趋势 → 硬币异常检测，三类硬币数据连续排列
- **检测结论百分比例统一** — 「随机性检测」和「偏斜检测」结论统一标注为「异常率」，始终显示百分比
- **期望最长说明优化** — 改为「纯随机上限」并加括号注释，更易理解

## v1.1.3 — 时间范围全面升级：一整周/一整月/自定义 + 先后手总场数

### ✨ 新增功能

- **时间范围「一整周」和「一整月」** — 「本周」→「一整周」（周一起始），「本月」→「一整月」，选中后展开子列表显示该时间段内每一天的日期范围选项，点选后可切换到该周/月。当前周/月显示「本周」/「本月」标识
- **时间范围「自定义」** — 新增「自定义」按钮，展开后输入起始和终止日期，点「应用」后按自定义日期范围统计。若起始日期晚于终止日期自动互换
- **先后手对比显示总场数** — 统计面板「先后手对比」卡片中先手/后手各自显示「共 X 场」

### 🔧 改进

- **吓跑对手文案修正** — 卡片副标题「开局对手吓跑次数」改为「吓跑对手次数」
- **时间范围子列表机制** — 一整周一整月类比一天的日期选择模式，从有数据的日期中按周/月分组展示可点击列表，无数据的当前周/月也始终显示
- **当前周/月默认选中** — 切换到一整周或一整月时，默认高亮当前周/月
- **各面板标题栏同步** — 统计/图表/显示/循环面板标题栏显示本周/本月时追加「本周」/「本月」标识

### 🐛 修复

- **自定义日期范围 end 计算** — 修复 `setHours` 被 `setDate` 覆盖导致结束日期不准确的问题
- **todayStr 函数截断恢复** — 批量替换 control.html 时 function 声明被截断，已修正

### ✨ 新增功能

- **各卡组胜率趋势** — 详细统计面板新增「各卡组胜率趋势」卡片，按卡组分组的累计胜率曲线，支持勾选/取消单个卡组，一键全选/取消全选
- **特定卡组胜率趋势** — 轮播面板新增「特定卡组胜率」项（在设置中选择要跟踪的卡组，显示总胜率+最近15场圆点），图表面板新增「特定卡组胜率趋势」页（下拉选择卡组）
- **硬币胜率趋势曲线** — 统计面板/轮播面板/图表面板新增硬币正反累计胜率曲线，每场一个累计数据点（金线），Y轴每10%一个刻度 + 50%对比虚线
- **总胜率趋势改为全量数据** — 胜率趋势曲线从最近10场改为使用全部对局数据绘制累计曲线
- **总胜率圆点** — 轮播面板总胜率下方显示最近15场胜负小圆点
- **胜率趋势/硬币趋势背景提亮** — 所有曲线图区域背景色从 `0.06` 提亮至 `0.12`

### 🔧 改进

- **晋级赛/保级赛拆分为独立面板** — 轮播面板中不再挤在一起，各自独立展示
- **图表面板顺序调整** — 硬币胜率趋势 → 胜率趋势 → 特定卡组胜率趋势 → 自用卡组胜率 → 对战卡组胜率（相邻排列）
- **「对面被直接吓跑」逻辑修正** — 改为只依赖是否选择「胜利」，不再要求先手
- **「一天」模式日期列表** — 当天无数据时依然显示在列表中，保证可选
- **图表面板字体全面提亮** — 所有灰色文字改为白色（`--text-dim: #ffffff`），坐标轴标注、图例、信息面板全部提亮
- **程序全局字体提亮** — CSS 变量 `--text-dim` 从 `#8888a0` → `#ffffff`，各处硬编码灰色同步改为白色
- **卡组胜率图表中文字体修复** — Y轴卡组名指定中文字体族，不再回退到宋体
- **圆环中心文字优化** — 先后手终场、后手突破、胜负分布、硬币统计、抽干牌组的圆环中心文字改为白色+阴影+chartArea居中，不再与色块重叠
- **控制面板循环设置顺序** — 选项列表顺序与轮播面板实际显示顺序一致

### 🐛 修复

- **统计面板所有数据不显示** — `new Chart()` 缺少闭合 `)` 导致整个脚本解析失败
- **控制面板所有按钮失效** — `forEach` 回调后多一个 `});` 导致大括号不匹配
- **取消全选按钮失效** — 区分首次加载（默认全选）和用户操作（保持清空）
- **下拉框看不清** — 轮播设置和图表面板中卡组选择下拉框改为浅底深字
- **曲线图滚动失效** — 图表面板交叉统计页面最终移除（数据量过大时难以在图表中有效展示）

## v1.1.1 — 遇到大牌哥统计、一天时间范围、卡手拆分为原因+制裁

### ✨ 新增功能

- **遇到大牌哥** — 循环面板、晋级/保级赛统计、图表面板（独立仪表盘+信息面板）新增「遇到大牌哥」统计项，显示次数和遭遇率；统计面板独立卡片，标题改为「遇到大牌哥情况统计」
- **被发牌员制裁情况** — 图表面板（柱状图+信息面板）/ 统计面板（卡片内独立小节）/ 轮播面板（新增幻灯片）新增独立展示「完全动不了」和「互卡」次数及占比
- **「一天」时间范围** — 「今天」扩展为「一天」，选中后展开日期列表（按对局记录倒序），点击具体日期精确过滤到该天；当天显示 `「今天」` 标识；所有面板标题栏同步显示选中日期
- **双方都动不了→自动联动** — 勾选「双方都动不了」时自动勾选并锁定「手牌动不了」，取消「双方都动不了」后手牌动不了恢复自由操作

### 🔧 改进

- **卡手原因面板重构** — 图表面板/统计面板/轮播面板统一将卡手拆分为两大部分：
  - 卡手原因：卡废件 / 卡同名牌 / 卡后手牌
  - 被发牌员制裁：完全动不了 / 互卡
- **卡手率统计范围扩大** — `totalCantPlay` 改为统计全部 5 种卡手情况的并集（完全动不了 / 废件 / 同名 / 后手牌 / 互卡），同一场多项只计一次
- **手牌动不了重新加入** — 控制面板新增「手牌动不了」勾选项（与废件/同名/后手牌并列），所有面板同步恢复统计
- **大牌哥文案统一** — 图表面板「对手大牌」→「遇到大牌哥」；晋级/保级赛「大牌率」→「遇到大牌哥」；统计面板卡片标题「对手大牌统计」→「遇到大牌哥情况统计」
- **T0动文案修正** — 所有面板「被T0最多的对手卡组」→「遇到T0动最多的对手卡组」
- **时间范围默认选中今天** — 切换到「一天」时自动选中当天，标题栏立即显示日期
- **版本号动态读取** — 关于面板版本号通过 IPC 从 `package.json` 实时读取，今后只需改一处
- **轮播面板字体调小** — 先后手对比数字 52px→40px；自用/对战卡组行 20px→16px（卡组名14px/数字15px）；对阵交叉统计行 20px→13px（卡组名12px/胜率13px）
- **轮播面板 emoji 调小** — 吓跑对手 🏃 和严重失误 💢 从 80px 降至 36px，防止溢出面板
- **统计面板卡手卡片结构调整** — 新增「卡废件/卡同名牌/卡后手牌」小节和「完全动不了/互卡」小节，原「双方都动不了」行改为「互卡」

### 🐛 修复

- **严重失误按卡组分布显示 undefined** — `d.count` 改为 `d.total`（数据结构中字段为 `total`）
- **对手T0按卡组分布显示 undefined** — `topD[i].count` 改为 `topD[i].total`
- **完全动不了占比显示错误** — 原先显示的是总卡手率（含全部类型），改为显示自身占比 `(cantPlay / total)`
- **clearForm 误伤时间范围按钮** — 提交后 `clearForm()` 的 `.btn-group .btn` 选择器清空了时间范围按钮的 `active` 状态
- **关于面板版本号未同步** — 写死的 `v1.0.2` 更新为 `v1.1.1` 并改为动态读取
- **先手VS后手幻灯片溢出** — 数字和胜率字号过大超出面板左右边界

## v1.1.0 — 图表全面增强 + 晋级/保级赛统计

### ✨ 新增功能

- **晋级赛 / 保级赛** — 控制面板录入新增「对局类型」选择（普通/🔥晋级赛/💧保级赛），后端独立统计各类型数据。详细统计页面、图表页面（第22张）、轮播面板均有独立项目展示
- **图表页面新增 4 张幻灯片** — 概览大数字 / 抽干牌组统计 / 对阵交叉统计（分组柱状图）/ 晋级保级赛对比，总数从 18 提升至 22
- **对手大牌统计独立卡片** — 详细统计页面中对手大牌从卡手卡片中拆分，新增独立卡片展示次数、占比、参考对比
- **胜率趋势折线图** — 详细统计页面新增全宽 Chart.js 折线图（与图表页同步），替代原有的彩色圆点展示
- **对阵交叉统计图表化** — 图表页面的对阵交叉从纯表格改为 Chart.js 分组横向柱状图

### 🔧 改进

- **手坑分布 / 卡手原因改为柱状图** — 这两类数据多项可能在同一局同时发生（如同时吃G+陨石），饼图误导互斥性，已改为横向柱状图独立展示次数
- **15 个图表增加信息面板** — 胜负（中心胜率）、先后手（平局/异常分别列出）、手坑（精确计数+吃G/吃陨率）、终场（正常率进度条）、突破（成功率+突破后胜率）、硬币（硬币胜率+占比）、卡手（bothStuck+bigHand+各比率）、失误（失误率+按卡组分布）、T0（胜率+按对手卡组分布）、吓跑（占比+按卡组分布）、提丰（汇总）、掉线/超时（比率+按卡组分布）
- **两个卡组胜率图 tooltip 增强** — 增加 W/L 场次显示
- **胜率趋势增加信息面板** — 显示最近 N 场胜率 + 彩色结果圆点
- **对手大牌增加信息面板** — 显示占比进度条 + 关联卡手次数
- **卡手卡片精简** — 对手大牌行移至独立卡片

### 🖼️ 展示优化

- **胜率趋势全宽** — 详细统计页改为占满一行的全宽卡片（180px 图表高度）
- **所有面板数据同步** — 图表页 / 轮播面板均可展示晋级保级赛统计

### 🐛 修复

- **胜率趋势时间范围切换异常** — 从"今天"切回"全部"后 canvas 被销毁导致一直显示"无数据"，改为独立消息层，canvas 常驻 DOM
- **DOM 结构错误** — 删除旧版半宽卡片时残留多余 `</div>`，导致趋势及以下模块全部不显示

### ✨ 新增功能

- **时间范围过滤** — 控制面板新增「统计时间范围」选择器（全部/今天/本周/本月），所有面板（显示/详细统计/图表/轮播）同步生效，数据按时间维度重新计算
- **抽干牌组统计** — 对局情况新增「抽干牌组」选项及子选项（对手/自己），详细统计和轮播面板均有独立项目
- **后手突破后胜率** — 新增「突破后胜率」指标（成功突破后的胜场数/成功突破次数），替代原有的 OTK 统计

### 🔧 改进

- **「停牌」→「没做出来」** — 终场选项文案统一修改
- **突破选项新增「不需要」** — 后手突破加入「不需要」按钮，对手无场面时选择，自动跳过 OTK/残局阻抗填写
- **后手突破成功率重算** — 分母改为「总后手场数」而非「尝试突破的次数」，更准确反映突破能力
- **轮播面板显示补全** — 手坑统计：从3项扩展到7项（全部手坑种类均显示）；先手终场：显示全部4项百分比；后手突破：显示突破后胜率
- **轮播面板字体调小** — 手坑统计 24px→20px，终场情况 28px→22px，防止内容溢出
- **卡组TOP3排除「未知」** — 轮播面板中自用/对战/交叉统计 TOP3 跳过名称为「未知」的条目
- **对手T0文案统一** — 所有面板中「T0」统一为「对手T0动」
- **所有面板显示时间范围** — 显示/详细统计/图表/轮播面板分别显示当前生效的时间范围标签
- **启动提示更新** — 提示文本改为导入导出备份说明，按钮为「确定」/「下次不显示」
- **后手突破统计行加后手总数** — 轮播面板中与先手终场对应，显示「共X场后手」

### 🐛 修复

- **吃G统计始终为0** — 存储字段名 `gotMaxxC`（大写C）与代码 `gotMaxxc`（小写c）大小写不一致，导致所有吃G记录未被统计，已全面修复并迁移历史数据
- **「大宇宙」→「大宇宙人」** — 轮播面板手坑统计中文案错误

### 💥 破坏性变更

- **数据存储改为仅系统临时目录** — 移除 `data/` 本地持久化，所有数据仅存于 `%TEMP%/md-stats-data/`，启动时不再从 `data/` 读取。如需备份请使用控制面板的导出功能

## v1.0.0 — 数据可视化图表 + 循环显示面板

### ✨ 新增功能

- **独立图表窗口** — 点击控制面板「📈 图表」按钮弹出，18 种可视化图表（饼图/柱状图/折线图/半环表盘）
- **循环显示面板** — 始终置顶的循环展示窗口，支持 19 种统计项的配置化循环播放（从控制面板的⚙设置中选择要循环的项）
- **完整图表覆盖** — 胜负分布、先后手对比、手坑分布、先手终场、后手突破、胜率趋势、自用卡组胜率、连胜状态、硬币统计、卡手原因、严重失误、对手T0动、吓跑对手、提丰统计、对战卡组胜率、掉线统计、超时统计、对手大牌
- **对手吓跑统计** — 记录对手开局逃跑的次数，按自用卡组分布

### 🔧 改进

- **Chart.js 路径修复** — `chart.html` 中 chart.min.js 加载路径从 `../lib/` 修正为 `lib/`，修复图表不渲染的 Bug
- **按需渲染策略** — 图表窗口改为只渲染当前活动页，导航时按需渲染
- **窗口 resize 自适应** — 图表窗口大小变化时自动重新渲染当前图表
- **全部窗口无边框化** — 详细统计窗口、控制面板窗口均改为无边框（frameless），添加固定标题栏和关闭按钮
- **字体统一提亮** — 循环显示面板小字颜色从 `#666680` 提亮至 `#a0a0b8`；统计面板 `--text-dim` 从 `#8888a0` 提亮至 `#a0a0b8`
- **概览数字布局** — 五个概览大数字（总对局/胜场/负场/平局+异常/胜率）改为单行紧凑布局
- **标题栏固定** — 详细统计窗口和控制面板改为固定标题栏（`position: fixed`），内容区域独立滚动
- **控制面板标题栏按钮** — 保留详细统计/图表/导出/导入/重置按钮并添加 title 悬停提示
- **循环面板补充** — 循环显示面板新增 先手终场/后手突破/卡手率/严重失误/对手T0/掉线/超时/提丰/自用卡组/对战卡组/交叉统计 等多个渲染器
- **示例数据** — 附带 10 条示例对局数据，克隆后 `npm start` 即可体验全部功能

### 🐛 修复

- 图表面板 Chart.js 因路径错误不加载，图表不显示的问题
- 打包后的 exe 中 Chart.js 同样因路径问题不工作


